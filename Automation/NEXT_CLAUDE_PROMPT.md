# NEXT CLAUDE PROMPT

상태: `READY_FOR_CODEX`

## 스펙 031 조사 보완 라운드 1 완료 — Codex 재검토 대기

Claude Code가 2026-07-31에 Codex 지적을 허용된 문서 안에서만 보완하고 문서 전용 커밋으로 일반
fast-forward push했다. 기준 `33323dd`.

### 지적은 유효했다

조사가 "인쇄에 시계가 없다"는 **코드 사실**에서 곧바로 **"구조적 불일치·결함"** 판정과 **"인쇄 포함"**
권장을 도출했다. `admin:335`("⏰ 템플릿용 시계 **가이드**")·`admin:342`("템플릿 제작 시 시계를
**미리 보면서 위치를 잡고**")는 시계가 완제품의 **물리적 하드웨어**일 가능성을 지지한다.

### 보완 결과 — 제품 의미는 `UNCONFIRMED`

보고서 §3.5.1에 양쪽 근거를 경로·라인과 함께 정리했다.

- **A(물리적 하드웨어)**: "가이드"라는 명시적 표현 · 시계를 보며 아트를 배치한다는 안내(회피 대상) ·
  운영자가 올리는 시계 **사진**(`customImg`) · **독립 인쇄 구현 2개**(V36 `:9732`, V365 `:11404`)가
  **모두** 시계 제외 · **주문 payload에 시계 상태 없음** · **"시계액자"가 상품군 이름**
- **B(인쇄 그래픽)**: 고객 문구 "시계 **추가**" · 고객이 요소 리스트에서 **삭제 가능** ·
  **`clockOn`이 `space-scene-v1`의 `design`에 저장** · 사이즈별·템플릿별 3단 위치 데이터 ·
  "미리보기 전용은 DOM 오버레이로 분리"하는 **선례가 있는데도** 시계는 캔버스에 baked
- **C(NOT FOUND)**: 하드웨어 어휘(무브먼트·바늘·초침·타공·벽시계·건전지)가 두 운영본 HTML과 `docs/`
  전체에서 **0건**이고, "시계를 인쇄한다"는 문서·주석도 **없다**

→ **"구조적 불일치"·"결함"·"인쇄 포함 권장" 단정을 §0·§3.5·§10에서 제거**했다.

### 재구성한 결정 순서와 범위 분기

- **F-4 제품 의미**(ⓐ 하드웨어 / ⓑ 인쇄 그래픽 / ⓒ 둘 다) → **F-4a**(하드웨어면 print 미포함 유지,
  preview 전용) / **F-4b**(그래픽이면 포함 여부) → **F-5**(F-4b일 때만 시각 의미)
- **§8.4 신설**: 하드웨어로 확정되면 print/export와 공유할 **결정적 plan을 전제하지 않고**
  preview overlay 계약과 **timer 정리**만 다룬다 → `packages/render`는 텍스트 때문에만 바뀐다
- **C-8**을 F-4 종속으로 변경, **STOP 조건 11·12** 추가
- 최소 구현 순서에서 **1~5(텍스트)는 F-4와 무관하게 진행 가능**하고 **6(시계)만 차단**된다

### 바꾸지 않은 것

§1·§2 textZones 조사 전체, §4~§7, C-1~C-7·C-9~C-11, §9 검증 설계.

### 게이트

문서 전용. 제품 코드·테스트·CSS·설정·manifest·`package.json`·`pnpm-lock.yaml` diff **0**,
신규 의존성 0, network·live·Firebase·CORS·Rules/Hosting·deploy **0**, `git diff --check` clean,
HEAD=origin, ahead/behind 0/0. 알려진 스펙 018 PNG 2개는 restore·checkout·stage·commit하지 않았다.

## Codex 다음 작업

보완된 보고서를 재검토한다. **F-4(시계의 제품 의미)는 Founder 결정이 필요하며 Claude가 확정하지
않는다.** 추가 근거를 찾을 경로를 알고 있다면 지시해 달라(예: 운영 카탈로그·상품 페이지·주문 이력 등
이 저장소 밖 산출물).

## Claude 다음 작업

**없다.** 구현 계약이 저장소에 기록되고 상태가 `WAITING_FOR_CLAUDE`로 바뀌기 전까지 텍스트·시계 관련
제품 코드·테스트·CSS·설정을 작성하지 않는다. 특히 **F-4가 확정되기 전에는 시계 구현을 시작하지 않는다**.
알려진 스펙 018 PNG 2개는 계속 손대지 않는다.
