# NEXT CLAUDE PROMPT

상태: `READY_FOR_CODEX`

## 스펙 032 조사 보완 라운드 1 완료 — Codex 재검토 대기

Claude Code가 2026-07-31에 Codex 지적 3건을 조사 문서와 상태 문서에만 반영하고 문서 전용으로
일반 fast-forward push했다. 기준 `5a42b29`. **제품 코드 변경 0.**

### 보완 내용

1. **★ C-1의 `lines` 재사용 경로가 현재 API에 없다** — `FrameTextZoneInput`에 `lines` 입력이 없고
   빌더는 값이 있으면 항상 `measureText`로 재wrap하며, `lines`는 `draw-text` command의 출력으로만
   존재한다. **"추가 계약 없이 가능"이라는 단정을 제거**하고 §8.1을 **후보 A/B/C 비교**로 재작성해
   각 후보가 **줄바꿈·회전·pan·레이어 순서** 네 불변식을 어떻게 보장하는지 표로 명시했다.
   새 근거: executor 헤더의 "transform은 caller의 몫"과 **`surface.ts`가 이미 `setTransform(dpr)` 후
   같은 plan을 실행**한다는 선례 → **후보 A가 가장 강하나 자간 품질은 NOT VERIFIED**. 선택은 C-1.
2. **P-5 분리** — 색·사진 transform / 시계 유무 / 고객 문구 원문을 나누고 각각 PNG 포함·로컬 저장·
   주문 전송·보존 기간을 구분했다. **최소안 P-5c: 고객 문구는 텍스트로 저장·전송하지 않는다**
   (이미 PNG에 픽셀로 포함). 별도 개인정보 정책 승인 없이는 범위 제외.
3. **P-4 출처** — 레거시 수치에 **인쇄소 근거가 없음**을 명시하고 **P-4a(임시값 + 실제 업로드·주문·
   배포 차단)** 와 **P-4b(확인 전 구현 보류)** 로 분리. STOP 조건 12·13 추가.

바꾸지 않은 것: §1~§7 레거시 조사 결과, §10 최소 구현 순서, C-2~C-8.

## Codex 재검토 요청

보완된 §8.1(후보 A/B/C)·P-4·P-5를 검토하고 다음을 판정해 달라.

- **C-1**: 인쇄 좌표를 얻는 방법으로 **A / B / C 중 무엇을 택할 것인가**
- 보완된 **Founder 질문 P-1~P-6**을 승인할 것인가(승인 후에야 Founder가 확정한다)

## Claude 다음 작업

**없다.** Codex 판정 전까지 저장소를 수정하지 않는다. 추가 `CORRECTION_REQUIRED`가 오면 지정된 범위만
보완하고(최대 3회 중 1회 사용), 구현 계약이 올라오면 허용 파일 안에서만 착수한다.
알려진 스펙 018 PNG 2개와 content diff 0인 `packages/render/src/plan/index.ts`는 계속 손대지 않는다.
