# 2026-07-14 핸드오프 — 전체화면 튐 완결(세로✅/가로 검증대기) + 브랜디드 커버 + 다음=최적화(배치4)

> 소비자/관리자: **https://design.dennproducts.com** (루트 `/`→denn-mockup-tool.html, 어드민=`/denn-admin.html`)
> 배포: `$env:PATH+=";$env:APPDATA\npm"; $env:FIREPIT_VERSION="1"; firebase deploy --only hosting --non-interactive` (프로덕션)
> wip 테스트: `firebase hosting:channel:deploy wip --expires 30d --non-interactive` → **https://denn-products--wip-cxz2mnnb.web.app**
> ★자동정책이 firebase deploy를 막음 → 사용자가 프롬프트에 `!` 붙여 직접 실행. firebase=`%APPDATA%\npm`(PATH추가), 로그인 sorrow6970@gmail.com.
> ★캐시: wip 검증은 **매번 새 `?v=숫자`**(모바일 캐시). 진단=`?fsdbg=1`. 실기기=카카오 인앱(CSS전용 FS).

---

## ⚠️ 배포 상태
- **프로덕션 라이브 = 커밋 `72cdfa8`**(세로 scroll-pin 초기버전, 진단 없음) + 배치1·2·3.
- **main/origin HEAD = `7825b1f`**(브랜디드 커버 + 가로↔세로 pin + 진단 오버레이 포함). **프로덕션 미배포.**
- wip 채널 = HEAD 최신(`714o`).
- 즉 브랜디드 커버·가로 수정은 main엔 있으나 프로덕션엔 없음. **가로 검증 완료 후** 진단 제거→프로덕션 배포.

## ✅ 이번 세션(07-14) 완료
### 세로 전체화면 튐 — 완전 해결(프로덕션 라이브 `72cdfa8`)
- 원인=캔버스크기 일정, **스크롤 위치가 노출 후 리셋됨**(온스크린 `sT`로그로 확정). [[project_mobile_pc_guide_settings_attempt]] 07-14 항목 참조.
- 해결=`dennFsRevealV`: 마스크 최소 420ms + scroll-pin(순수관찰 게이트 + scroll-event 동기복귀).

### 브랜디드 커버 + 가로 이식(main `7825b1f`, 프로덕션 미배포)
- **DENN PRODUCTS 커버**(`#denn-fs-cover`): 흰 화면 대신 최초 로딩 스플래시(`#denn-boot-cover`)와 동일 룩. `.dbc-logo`/`.dbc-spin` **재사용만으론 스타일 안 먹음**(선택자가 `#denn-boot-cover .dbc-logo`로 부모 스코프) → `#denn-fs-cover`에 명시 필요. **화이트 2톤 테마**(`:root` L13727이 `--cream/--warm/--gold`를 흰/회색으로 remap) → 커버도 회색/흰색(사용자가 웜/골드보다 흰색 선호). 세로+가로 공용.
- **가로/회전(dennGatedRevealV)에 scroll-pin 이식**: `sT`로그 확정 — 가로→세로 복귀 시 마스크 걷힌 뒤(+16563 sT28 [----]) 다음 프레임(+16623 sT0)에 브라우저가 스크롤 리셋 = "뒤늦게 떨어짐". → dennFsRevealV와 동일한 scroll-event pin(`__gOnScroll`), 노출 후 1s 유지. 가로 중엔 `dennPortraitFrameAnchorTopV=-1`이라 no-op.

## 🔧 미검증(다음 세션 첫 확인)
- **가로→세로 복귀 pin(714o)**: 실기기 미검증. `?fsdbg=1&v=714p`로 가로 전체화면→세로 복귀 영상 → `sT`가 노출(`[----]`) 후 앵커값에서 0으로 안 떨어지면 성공.
- 검증되면: **진단 오버레이 제거**(파일 끝 `?fsdbg=1` script + `FSVER`) → 커밋 → **프로덕션 배포**.

## 🧭 다음 핵심 작업 = 예전 최적화(오픈전 배치4)
[[project_prelaunch_bug_audit]] 백로그. 배치1(라이브)·2·3(main) 완료, **배치4 미착수**:
- #1 세로저장 revert 지뢰밭(무한재귀 금지 — [[project_mobile_pc_guide_settings_attempt]] 06-30 교훈)
- #4·#5·#12 회전/드래그/사이저
- #18 클라우드 RMW(아키텍처변경)
- PLAUSIBLE 11건. findings.json + `docs/2026-07-13-prelaunch-cleanup-part2-handoff.md`.

## ★디버깅 방법론(이번 세션 핵심 교훈)
- **회전/FS 튐은 정적추론 반복 실패 → 온스크린 `sT`(scrollTop)+`FSVER`(버전스탬프)+플래그(`[FAER]`=Fullscreen/Active/Entering/Rotate) 진단으로 즉시 확정.** 영상은 ffmpeg(imageio-ffmpeg pip)로 프레임추출→콘택트시트, 전환은 15~20fps 확대, 로그는 풀해상도 1프레임(누적 로그라 전체 시퀀스 읽힘).
- **튐의 정체(3가지 다른 원인 오진 후 확정)**: 캔버스 크기 아님(411x890 일정) → **스크롤 위치 리셋**. rAF 교정은 다음 프레임이라 1프레임 번쩍 → **scroll-event 동기복귀**가 정답. pin만으론 부족(리셋이 노출 후 지속) → 마스크 최소시간 + 불투명 커버 병행.
- **세로≠가로 경로**: 세로=`dennFsRevealV`(fsPBtn), 가로/회전=`dennEnterFsLandscapeV`→rotate onChange→`dennGatedRevealV`. 수정은 각각 이식해야 함.
- 커밋 전 `synccheck.py`(이전 세션 scratchpad)로 인라인 스크립트 구문검증. wip 검증은 FSVER 스탬프로 캐시 판별.
