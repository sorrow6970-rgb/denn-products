# 2026-07-03 세션 핸드오프 — Firebase 배포 · 보안B · 커스텀도메인 · 발행본 치명버그 해결

> 소비자 라이브: **https://design.dennproducts.com** / 관리자: **https://design.dennproducts.com/denn-admin.html**
> (Firebase 기본: denn-products.web.app 도 동일하게 동작)

---

## 오늘 한 것 (전부 라이브 배포됨)

### 1. Firebase Hosting 배포 + 방식B 자동발행
- `firebase login`(sorrow6970@gmail.com) → `firebase deploy --only hosting`. HTML 2개(admin/mockup), HTML no-cache.
- 관리자 **📢게시**(`dennPublishState`) → `published/state.json`(Storage) → 소비자 부팅 fetch로 반영.
- ★`.ps1` 실행정책: `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned`. `!`세션은 non-interactive라 login 실패 → 실제 PowerShell 창 필요.

### 2. 보안 B — 운영자 Firebase Auth + 관리자 클라우드 동기화 (`denn-admin.html`)
- **로그인**: 로컬 하드코딩 비번(denn2024) → `signInWithEmailAndPassword`. 익명 제거, non-anon만 `_ready`. 세션 지속(재방문 자동진입, `dennOnAuthStateV`). **계정은 Firebase 콘솔서 생성**(Email/Password 공급자 켜기 + Users 추가).
- **클라우드 동기화**: Storage `admin/state.json`(비공개). `dennCloudSaveAdminV`(저장 펀넬 훅, 디바운스 3s) + `dennCloudLoadAdminV`(로그인 시 `getBytes`). 충돌=`__cloudRev` 최신우선 + `mergeAdminStateSafe`. 하이드레이션 가드(`__dennHydratedV`)로 로컬 클로버 방지. → **어느 기기서든 로그인=최신 관리자 데이터.**
- **storage.rules(보안B)**: ★Firebase는 겹치는 match를 OR → catch-all `read:if true`가 admin/까지 노출. **경로별 명시 match**로: admin/(op()만), temp-share·proofs(익명write), published·templates·placeholders·guides·mockups·editor-overlays(공개read+op()write). `op()=auth!=null && sign_in_provider!='anonymous'`. read조건에 request.resource.size 금지. 검증: admin/=403·published/=200.
- 개념: **💾저장=로컬+클라우드백업**(운영자 기기동기화) / **📢게시=소비자 공개**.

### 3. 커스텀 도메인 `design.dennproducts.com` (SSL✓)
- `.shop`(dennproducts.shop)은 만료 방치 → 운영중 자사몰 `.com`의 **서브도메인** 사용(루트 자사몰 무영향).
- ★★함정: **카페24 네임서버가 모든 이름(apex/서브/와일드카드)에 파킹 A레코드 무조건 주입**(NS마다 다른 IP, DNS관리UI·파킹해제와 무관). A레코드는 주입 섞여 실패.
- **해결=CNAME**(`design`→`denn-products.web.app`): CNAME은 A와 공존 불가라 리졸버가 CNAME 우선·주입A 무시 → 깨끗. (Firebase 서브도메인은 CNAME만 제공.)
- 완전 클린은 .com Cloudflare 이전뿐이나 자사몰 리스크로 보류. 공개리졸버는 정상.

### 4. ★★★ 발행본 데이터 안 뜨는 치명 버그 해결 (신규 소비자 전원 영향) — `denn-mockup-tool.html`
- **증상**: localStorage 있는 브라우저만 정상, 시크릿/신규/모바일은 fetch 됨(490943b)인데 denn_admin=1037B·ADM=0sizes.
- **근본원인**: 발행본 apply가 `setItem+saveAdminToIdb→location.reload()→dbGet` 라운드트립 의존. 시크릿/모바일서 reload 미발동 or IDB 커밋 실패 → 490KB 유실. + 첫 로드 빈 ADM으로 여러 load 핸들러(saveSettingsV33/loadAdminFresh→persistAdmin/writeAdminV48)가 기본값 1037B 저장, 490KB와 경쟁해 덮음. (기존데이터 프로필은 실데이터라 안덮여 정상=깨끗한 환경만 깨짐.)
- **수정**:
  - (a) 발행본 객체를 `window.__dennPublishedDataV`로 **메모리 직접 전달** + `window.__dennPubReadyV` promise, **리로드 완전 제거**. init(load, `denn-mockup-tool.html` L~2361)이 promise await 후 메모리 데이터를 바로 ADM으로 사용(IDB/reload 무의존).
  - (b) `window.__dennPubLoadingV` gate를 **부트 IIFE 시작부터 true**, init이 실데이터 확정 직후에만 false. 그 전 모든 기본값 저장 4경로(saveCustomerAdminStateToLocalStorage/writeAdminDbV48/persistAdmin) 차단. 안전TO 10s.
  - (c) **자가치유**: 가드(`denn_pub_synced`) 켜졌어도 로컬 denn_admin에 frameSizes 없으면(무효) 가드 무시 재fetch → **이미 오염된 기기도 새로고침만으로 자동 복구**.
- **검증**: 시크릿 초기화후 콘솔 `[boot] ADM set from published | frameSizes=9`, LS=490943·ADM=9sizes. ★부수효과=첫로드 리로드 깜빡임/느림 해소.
- 진단: `?debug=1`(부트 상태박스), `?share=published/state.json`(가드 우회 강제로드).

### 5. 기타 목업 수정(오늘)
- 소비자 부트 상태박스 숨김(`?debug=1`만 표시). 이미지 팬 존 클램프. 프레임 캔버스 `touch-action:none`(모바일 주소창 방지). 모바일 액자 스케일 상한(`MOB_CAP=0.9`=화면꽉참)+데드존 제거(슬라이더 max 동적)+매 로드 135% 리셋. 이미지 팬 중 스케일 흔들림 방지(`__dennFramePreviewFreezeV`). 하단앵커 refH 폴백(가이드사이즈 높이).

---

## ▶ 다음 작업 (우선순위 순)

### ⚡ 2026-07-03(2) — 실기기 버그 A·B·C **수정 완료(코드)** · 배포/실기기 검증 대기

> ★**배포 상태**: A·B·C 코드는 커밋·푸시됨. **아직 라이브 미배포** — 작업하던 이 PC엔 node/firebase-tools 미설치(번들 node.exe만 있고 `firebase.cmd` 없음). **firebase 되는 PC에서 `git pull && firebase deploy --only hosting`** 후 실기기 검증 필요. (`firebase login`은 인터랙티브라 Claude 세션 셸에선 불가.)
> ★**검증 순서**: (1) 두 호스트(design.dennproducts.com · denn-products.web.app) HTML에 마커 `__pubFetchOnce` 존재 확인(=새 코드 서빙) → (2) A=실패하던 폰 2대 / B·C=아이폰 실동작.
> ★확인된 것: Storage `published/state.json`은 커스텀 도메인 포함 **모든 Origin에 CORS `*`**(도메인 특이 차단 아님), 490KB·frameSizes=9 정상.

#### (A) 발행본 JSON 특정 모바일서 여전히 안 불러와짐 — #4 "해결"은 부분해결이었음 (`denn-mockup-tool.html`)
- **증상**: 특정 모바일 브라우저에서 열면 발행본 JSON 데이터가 안 뜸(빈 ADM). 데스크톱/일부 기기는 정상.
- **유력 원인**: `L762 setTimeout(__pubDone,8000)` — readiness promise(`__dennPubReadyV`)를 **8초에 강제 resolve**. `published/state.json`이 ~490KB인데 느린 모바일망(3G·혼잡 LTE·인앱 웹뷰)서 fetch가 8초를 넘기면, init(`L2376–2378`)의 `await`가 8초에 풀려 `window.__dennPublishedDataV`가 아직 `null` → `loadAdminState()` 폴백 → 신규/시크릿 모바일은 빈 데이터로 확정. 게다가 fetch(`L807`)에 **timeout/AbortController/retry 전무**(단일 fetch). 자가치유(`L800–803` denn_pub_synced+frameSizes 체크)는 **다음 새로고침에만** 동작 → "첫 진입시 안 뜸" 설명과 일치.
- **✅ 수정됨(코드)**: (a) `L762` 강제 resolve **8s→26s**, (b) 저장 게이트 안전해제(`L740`) **10s→28s**(fetch 창보다 뒤로 — 그 사이 기본값 저장 레이스 방지), (c) fetch에 **AbortController per-attempt 12s + 네트워크 오류/스톨 시 1회 재시도 + `cache:'no-store'`**(`L807~817` `__pubFetchOnce`/`__pubTry`). 404(발행본 없음)는 null→`.then`이라 재시도 안 함. → 느리지만 성공하는 fetch가 강제 resolve를 이기고, 스톨은 재시도로 복구.
- 남김(②로): 경량 version.json 선확인·payload 다이어트는 추가 최적화라 별개.
- 진단 재현: 저속 네트워크 스로틀 + 시크릿 모바일, `?debug=1` 부트 상태박스로 fetch 소요시간 확인.

#### (B) 아이폰 전체화면보기 버튼 무반응 (`denn-mockup-tool.html`)
- **증상**: iPhone(iOS Safari)서 전체화면보기 버튼 눌러도 아무 반응 없음.
- **원인**: 모바일서 실제 보이는 버튼은 **Button B `#denn-fs-enter-btn`**(`L14365` 생성, body에 append) → `dennEnterFsLandscapeV()`(`L14350`). 이 핸들러는 **네이티브 `requestFullscreen`/`webkitRequestFullscreen` + `screen.orientation.lock`만** 시도. iOS Safari는 비디오 외 요소 requestFullscreen 미지원 + orientation.lock 미지원 → `req` falsy, `lockLand()` no-op → **무반응**. 코드 주석(`L14349`)도 "iOS 미지원은 조용히 무시(폴백=물리회전)"라고 명시 = 의도된 무동작이지만 UX 불량. (Button A `openRoomFullscreenView` `L3612`는 CSS 폴백 있으나 모바일선 `hideFsBtn()` `L14375`로 숨겨져 안 쓰임.)
- **✅ 수정됨(코드)**: `dennEnterFsLandscapeV()`(`L14360`) — Android(`req`=네이티브 전체화면 존재) 경로는 그대로. **네이티브·`orientation.lock` 둘 다 없을 때만(=iOS)** `setRoomFullscreenLayout(true)`로 **CSS 세로 전체화면** 폴백 → 탭 시 실제로 커짐. 종료는 `denn-room-fullscreen-active`서 CSS(`L3119`)가 Button A를 '전체화면 종료'로 강제 노출 → `hideFsBtn` 인라인 숨김만 `removeProperty`로 풀어 노출 보장. 진입 후 `hint()`로 Button B 숨김.

#### (C) 내공간보기 전체화면 → 뒤로가기 후에도 전체화면 버튼 잔존 (`denn-mockup-tool.html`)
- **증상**: 내공간보기서 전체화면 진입 뒤 "뒤로가기"로 프레임 화면 복귀해도 전체화면보기 버튼이 계속 떠 있음.
- **원인**: 잔존 버튼 = **Button B `#denn-fs-enter-btn`** — `document.body`에 `position:fixed`로 append(`L14369`), 표시/숨김은 오직 `hint()`(`L14360–14372`)가 `isMob()&&roomOpen()&&…` 조건으로 계산. 뒤로가기 경로 `closeRoomMockup` 래퍼(`L4627–4628`) → base(`L1965`)는 `#room-modal`만 `display:none` + `setRoomFullscreenLayout(false)`만 하고 **`hint()` 미호출**. 버튼은 modal 자식이 아니라 body 자식이라 modal 숨겨도 안 사라지고, `roomOpen()`이 false여도 `hint()` 재계산이 안 돼 마지막 `display:flex` 상태로 잔존(다음 resize/orientationchange까지). `exitRotateFs()`(`L14327`)도 동일 누락.
- **✅ 수정됨(코드)**: `closeRoomMockup` 래퍼(`L4638`)에 종료 정리 추가(`prevCloseRoom` 조기 return 전에 실행) — `#denn-fs-enter-btn` `display:none`(hint는 다른 클로저라 id로 직접) + `denn-rotate-fs` 제거(✕=`denn-rotate-exit`도 CSS로 자동 숨김) + `__dennAwaitPortraitV`/`__dennAutoFsV108` 리셋 + 네이티브 FS 잔존 시 `exitFullscreen` + 회전 조절바 숨김. → 프레임뷰 완전 복원.

---

### ① 주소창 빈 공간 / 상단 간격 (dvh 레이아웃) — 매핑 완료, 바로 착수 가능
**증상**: 모바일서 브라우저 주소창이 나타났다 사라질 때 상단 메뉴(탭바)와 액자 미리보기 사이에 빈 공간 생김 + 상단 간격 어긋남.
**원인**: `#page-frame.main.on` 컨테이너 높이가 `svh`(주소창 있는 작은 뷰포트 고정값)라 주소창 토글 시 라이브 뷰포트와 안 맞음. 중앙정렬(justify-center) 목업이 어긋나며 빈공간. `95px` 헤더 상수도 실제(~60px)와 불일치.
**수정 (모두 `denn-mockup-tool.html`, `#page-frame` 스코프 — 케이스탭/룸모달 무영향)**:
- **L12672** `#page-frame.main.on{...height:calc(100vh - 95px);height:calc(100svh - 95px)...}` → `100svh`를 **`100dvh`**로, `95px`를 실헤더(~60px)로.
- **L2755/L2756** `#page-frame .preview-area{min-height:50vh!important}` / `46vh` → **제거**(L12673의 `flex:1 1 auto`가 이미 남은공간 채움) 또는 dvh화.
- 건드리지 말 것: `body{min-height:100vh}`(L65)·`.main{...}`(L80)=케이스탭 공유 / `denn-mobile-layout-v1`(L9665)=케이스 split-pane 공유 / `#room-modal`(별도 오버레이) / `#frameCanvas{touch-action:none}`(L12603, 유지).

### ② (선택) 소비자 JSON 로딩 추가 최적화
- 리로드는 이미 제거(4번). 추가로 **경량 version.json**(발행시각만) 먼저 확인 → 이미 최신이면 490KB 스킵(재방문 빠르게). payload 다이어트(폰트 base64 등).

### ③ (선택) 슬라이더 위치 readout desync
- 소비자 좌측 sg-ox/oy(위치 X/Y) 숫자가 로드/배경전환 시 튐(실제 렌더=image-anchor라 시각은 정상). Explore 확인: rmRender 끝(~L4262)서 `setVal('sg-ox-r',RM.pos.x*100)` 등 되써넣기(setVal은 이벤트 미발생=루프無/핀치無영향).

### ④ (선택) 카페24 파킹 완전 제거 — .com Cloudflare 이전
- 자사몰 DNS 전체 이전 리스크 크므로 여유·확신 있을 때만.

---

## 환경/재개 메모
- 코드 전부 GitHub push됨(`github.com/sorrow6970-rgb/denn-products`). 집/다른 PC에서 `git clone` + `npm i -g firebase-tools` + `firebase login`(sorrow6970@gmail.com) 하면 `firebase deploy --only hosting` 가능.
- 관리자 데이터=클라우드 동기화라 어느 기기서든 로그인=최신.
- ⚠️ Claude Code 작업 메모리는 로컬(.claude)이라 다른 PC엔 없음 — 이 핸드오프 + git 로그가 맥락.
- 배포 사이클: 로컬 수정 → (dev서버 실폰 검증) → `git commit` → `firebase deploy --only hosting` → 라이브 확인. 데이터 변경은 📢게시만.
